spring:
  cloud:
    gateway:
      routes:
        - id : r1
          uri : http://localhost:8091/
          predicates:
            - Path=/customers/**
        - id: r2
          uri : http://localhost:8092/
          predicates:
            - Path=/accounts/**
  application:
    name: gateway-service
server:
  port: 8888
